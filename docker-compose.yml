services:
  postgres:
    image: postgres:17
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: data_room
    ports:
      - '5432:5432'
    volumes:
      - data_room_pg:/var/lib/postgresql/data

volumes:
  data_room_pg:
